import { FormatCoordinatesPipe } from './format-coordinates.pipe';

describe('FormatCoordinatesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCoordinatesPipe();
    expect(pipe).toBeTruthy();
  });
});
